# 작업 규칙

## 1. 언어 (가장 중요)

- 사용자에게 보이는 **모든 글은 한국어로만** 쓴다.
  - 최종 답변뿐 아니라 작업 중간의 진행 안내, 도구를 쓰기 전 한두 줄 설명, 오류 설명, 질문까지 전부 포함한다.
  - 영어 문장을 한 줄도 섞지 않는다. 코드, 명령어, 파일 이름, API 주소만 원문 그대로 둔다.
- 커밋 메시지와 코드 주석은 영어여도 된다. 사용자에게 보여 주는 설명은 한국어로 한다.

## 2. 사용자 환경

- Windows PC, 명령은 **cmd** 기준으로 안내한다. (PowerShell 아님)
- 프로젝트 위치: `D:\AI개발\M\Maple`
- 개발 서버 주소는 `npm run dev` 실행 시 나온 주소를 쓴다. (5173이 사용 중이면 5174 등)

## 3. 프로젝트 원칙

- API 응답 구조를 추측하지 않는다. `npm run assets:test`, `npm run assets:inspect` 등으로 실제 응답을 먼저 확인한다.
- 이 작업 환경에서는 maplestory.io 접속이 막혀 있다. 실제 API 확인은 GitHub Actions(`.github/workflows/api-test.yml`)에서 한다.
- 수집한 게임 이미지와 데이터(`assets/`, `data/`)는 넥슨 저작물이므로 git에 올리지 않는다.
- API 서버에 부담을 주지 않도록 동시 요청 수와 대기 시간을 지킨다.
